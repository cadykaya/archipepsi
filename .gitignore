.archipelago/
.env
.godot/
__pycache__/
.pytest_cache/
*.tmp
*.bak
*.pyc
.venv/
saves/
# The pre-art playtest run's own save directory, kept apart from
# `saves/` so a baseline playtest cannot overwrite a real campaign.
playtest-2.5/
.integration-saves/
generation_archive/
godot-bin/

# Editable installs leave these behind; they are build output, not source.
*.egg-info/
