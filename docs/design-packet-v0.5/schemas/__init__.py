"""Archipepsi v0.4 binding schemas.

Copy this directory verbatim into the bridge. The modules import each other
relatively, so the package works both standalone (`pytest` in this folder)
and nested inside `archipepsi_bridge`.
"""
