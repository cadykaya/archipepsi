# Sources, preserved decisions and limits

## Latest source of completed work

`references/PROD_HANDOFF_330c555.txt` is the user's uploaded report, originally
`Pasted text(20260923-031003).txt`.

Its final handoff reports:
- frozen local verification at `57e962e`;
- final pushed handoff at `330c555` on `claude/archipepsi-0-4-blindside`;
- P14 normal-arrival plate/latch/door passage and actual bridge/client restart;
- ThemePack material propagation with test packs, cache/fallback/Hub/hazard checks;
- final 56/56 local steps and 1,933 Python tests;
- no ordinary art approval or production pack selection;
- no changes to consumables during that batch.

Earlier narration in the report contains preliminary suite totals. The work order uses the final handoff as the reported total and asks the next run to derive its own numbers from an actual command manifest. It does not independently reconcile or certify every earlier line.

The GitHub branch reference was read during packet preparation and resolved to:
`330c55563104e51b9d87a1c9c2efa9efe6529722`.

## Scope and design authorities consulted

The original `ARCHIPEPSI_0_4_OVERNIGHT_04_MASTER.md` was read in full. It defines the finite P00–P23 programme, including the accepted major/minors, transport, acquisition, manipulation, Status, signal, machinery and item work. Its original task packaging and old state claims are historical; Overnight 05 is the new continuation order.

Selected repository documents were read at the same pinned head:
- `CLAUDE.md`: architectural authority split, generated artifacts, narrow reads and focused tests. Its generic heartbeat instructions are explicitly overridden by the owner's standing no-scheduling rule.
- `docs/ledgers/PROD_OV04.md`: older completed implementation slices and their scope limits. Later corrected claims supersede its older entries, including doorway Status cleansing and broad transport completion.
- `docs/design-proposals/06_THE_AMALGAM.md`: effective pin/modifier hierarchy; §§11.7, 13–15, 19.7, 20, 21 and relevant persistence/consumer rules.
- `docs/design-proposals/02_PHYSICS_IS_THE_GAME.md`: §10 pickup/drop/recovery and §14's exact twelve manipulation verbs, target/force/relationship restrictions and required-use qualification.

The historical huge-batch approval explicitly preserves full-strength featured acquisition: real reachable claim, qualifying local function, original foreign delivery, pre-seed AP representation and restart/delay safety. It does not authorize weakening the guarantee to finish M2.

## Exact manipulation vocabulary checked

PUSH, PULL, HOLD, ALIGN, TETHER, PIN, ATTACH, DETACH, ROTATE,
LIGHTEN_FIELD, ANCHOR_FIELD, SETTLE.

These names are source-derived. The order of implementing them in the new packet is a new execution recommendation. Ordinary hand carry is a separate §10 interaction, not a thirteenth Echo manipulation verb.

The effective Amalgam/landed Status semantics must be read with its inherited sections: a mass field changing kilograms is not the same operation as LIGHTENED changing semantic class. Do not import a superseded Design 2 statement about a similarly named effect as a new Status rule.

## Original minors

The files in `original_minor_specs/` were copied byte-for-byte from the already supplied `ARCHIPEPSI_FIRST_MINOR_SPECS_RECOVERED.zip`. They are continuity backups. If the repository has a newer owner-approved version, use it and record the difference. These backups do not prove runtime completion.

## Optional design research

Two user-provided reports are included:
- `Archipepsi_Room_Identity_Research.md`
- `Archipepsi_Spatial_Design_Investigation.md`

Their inspection snapshots are old. Use their situation/recognition/adaptation/consequence framework, controlled comparisons and playtest questions as design inputs. Do not adopt their old recommendations to delay the expanded roster/map or replace the approved 0.4 programme. Their external game studies were not re-verified in preparing this packet. No new universal SituationSpec framework is mandated here.

## What is newly proposed in this packet

The O05 ordering, operational stop conditions, temporary single-writer seam permission, staged candidate itinerary, per-package acceptance packaging and reserve queue are newly drafted execution instructions. They are not quotations from the Amalgam and do not pretend all remaining work is already authorized in a particular implementation shape. The owner grants this work order by sending its dispatch.

The packet does not choose unresolved economy, legacy-effect, same-Zone refill, return-later or live held cross-room policies. It permits implementation of settled semantics and requires exact blockers for unsettled ones.

## Review limitations

This preparation read the supplied report, the original work order and selected source documents. It did not clone/build/run the complete repository, independently execute Godot/Python tests, verify all remaining code paths, inspect the new ThemePack rendered sheet, or compare all old/new suites. Tasks therefore start with a short current-state reconciliation and do not assume every listed operation is missing.

No repository writes, agent invocations, schedules, billing changes or provider upgrades were performed. Generated files are a dispatch/review packet only.
