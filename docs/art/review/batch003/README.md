# Batch 003 — the Hub

**12 images.** The first production batch after Style Lock. Everything here
inherits approved DNA; nothing in it establishes new visual language.

## Start here

| | |
| --- | --- |
| **[`I_hub_from_spawn.png`](I_hub_from_spawn.png)** | The Hub as the player meets it, at its real 22 × 16 × 5 m, with every fixture where `hub.gd` puts it. Compare it against what the game draws today: untextured boxes and prisms with flat colours on them. |
| **[`I_hub_shop_wall.png`](I_hub_shop_wall.png)** | Walking up to the shop. The locked lighting rule doing its job — a cold room with one warm utility pool in it. |

## The rest

| Image | What |
| --- | --- |
| [`I_hub_greyscale.png`](I_hub_greyscale.png) | The spawn view without colour. |
| [`I_hub_back_wall.png`](I_hub_back_wall.png) | The portal and the Epsilon installation. |
| `H_hub_*.png` | The five Hub fixtures, one sheet each at 3–4 m. |
| `F_arch_*.png` · `F_hub_lab_doorway.png` | The three architecture pieces. |

## The one thing that needs an answer

The Epsilon installation's position in these shots is a **proposal**.
`hub.gd` has no fixture for it, and it clashes with the abandon console.
See `ART_REVIEW.md` and interface item 4 in `ART_FRONTIER.md`. Art is not
picking a resolution.
