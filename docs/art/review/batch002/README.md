# Style Lock Batch 002 — review images

**26 images.** Most of Batch 002 came back `PASS`; the Epsilon
installation took one targeted revision, and that revision is the only thing
still awaiting a verdict. The ledger is
[`../../ART_REVIEW.md`](../../ART_REVIEW.md) and every entry in it is
`PENDING`. Only the owner turns `PENDING` into `PASS`.

This is the second revision pass. The 001-R verdict was *close, but do not
unlock mass production yet*, and it asked for six things. Each has its own
section below.

## Start here — the 002-R revision

| | |
| --- | --- |
| **[`A_epsilon_operator.png`](A_epsilon_operator.png)** | The installation from where a person would stand: eye height, one pace back from the desk, aimed level so the desk top is visible. **This is the shot the revision exists for.** Does it read as a huge computer somebody operated? |
| **[`A_epsilon_fusion.png`](A_epsilon_fusion.png)** | Close, at the right-hand third — the mass over the desk, through the monitor housing, replacing the console's own panels. Is the takeover asymmetric enough? |
| **[`A_epsilon_in_room.png`](A_epsilon_in_room.png)** | The same object in a room, from the doorway, in the game's own camera. |
| **[`A_epsilon_oblique.png`](A_epsilon_oblique.png)** | From the alien end, looking back across the console. |
| **[`A_epsilon_value.png`](A_epsilon_value.png)** | The operator frame with the hue removed. If the split only exists in green it will not survive a dark room. |

Everything below is the batch as first reviewed. The five verdicts stand as
written; nothing in those sections was rebuilt except where the emissive
solve changed, which affected every lit surface in the project.

## 1 · Epsilon — a room-scale computer installation

| Image | What it answers |
| --- | --- |
| [`A_epsilon_installation.png`](A_epsilon_installation.png) | The full sheet — front, 3/4, side, rear, silhouette, clay, a 1.8 m scale rod, and 8 m play distance. |
| [`A_epsilon_installation_medium.png`](A_epsilon_installation_medium.png) | The same at 4 m: walking up to it. |
| [`A_epsilon_installation_close.png`](A_epsilon_installation_close.png) | And at 2 m: standing at it. Does the surface hold up? |
| [`A_epsilon_in_room.png`](A_epsilon_in_room.png) · [`_oblique.png`](A_epsilon_in_room_oblique.png) | In context, head-on and from across the room. |

The room is 12 × 12 × 4 m and the Hub is 22 × 16 × 5 m, so **it reads bigger
here than it will there**. The caption on the image says so too.

## 2 · Facility lighting — cold room, local warm pools

| Image | What it answers |
| --- | --- |
| [`I_room_utility_pools.png`](I_room_utility_pools.png) | The revision: ceiling stays cold, three small wall fixtures make warm pools. |
| [`I_room_utility_pools_greyscale.png`](I_room_utility_pools_greyscale.png) | The same without hue. If the pools only exist as brightness, the revision did not do what was asked. |
| [`I_room_utility_pool_near.png`](I_room_utility_pool_near.png) | Standing in one, with the fixture visible. |
| [`I_room_warmlight_rejected.png`](I_room_warmlight_rejected.png) | The 001-R version that warmed the whole room. Kept for the comparison and labelled as rejected — nothing is deleted here. |
| [`I_room_wide.png`](I_room_wide.png) · [`I_room_greyscale.png`](I_room_greyscale.png) | The cold room with no pools at all, as the baseline. |

## 3 · Portal — the split pushed

[`C_portal_b2_wound.png`](C_portal_b2_wound.png). B and B-R are both kept
and unmodified; this is a third reading, not a replacement.

## 4 · The enemy family

| Image | What it answers |
| --- | --- |
| [`D_enemy_family_18m.png`](D_enemy_family_18m.png) | Ten roles, two ranks of five, both at 18 m. True 1080p scale, then 2× nearest. |
| [`D_enemy_family_silhouette.png`](D_enemy_family_silhouette.png) | The same as black shapes. |

Each is labelled with its combat role. Seven of the ten are proposals with
no engine collision box; their manifests say `"engine_box": false`.

## 5 · The grapple anchors — both kept

| Image | What it answers |
| --- | --- |
| [`E_anchor_a_use.png`](E_anchor_a_use.png) | A soffit, on a ceiling at corridor height, swing passing under it. |
| [`E_anchor_b_use.png`](E_anchor_b_use.png) | B as a wall jib, arm out from the wall, swing running along it. |

Both carry a 1.8 m rod at the jump's 4.67 m flat reach and an orange bar at
its 1.33 m apex, because **an anchor that does not beat a jump is
decoration**.

## 6 · The family board (the optional item)

[`F_style_board.png`](F_style_board.png) and
[`F_style_board_greyscale.png`](F_style_board_greyscale.png). One row the
facility built, one row Epsilon did, one floor, one light, one camera.

## What is NOT here

No mass production. No new themes. Nothing rigged or animated — a telegraph
is a promise and a promise cannot be judged from a static model. No status
above `PENDING`.
