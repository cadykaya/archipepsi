# Wave 1 and the hall — promoted

**Arty** · art lane · branch `claude/archipepsi-art` · 2026-09-04

Four room shells from `review: "pending"` to `review: "pass"`, on the
owner's verdict. **All twelve authored room shells are now `pass`.**

---

## 1 · Head before

`f2b920a`

| authority | at |
| --- | --- |
| Production technical certification | `7e13f44` |
| independent audit | `f97545f7812d53ad6923b8a4c4ad0cf07925a667` |

## 2 · Canonical review source changed

`tools/export_content_pack.py` — the per-shell **`SHELL_REVIEW`** table.
Four values, plus the doctrine comment above it rewritten to record the
three authorities, the form conditions and their state, why the
physical-truth work did not reopen the review, and what `pass` does not
mean.

Also `tools/content/verify_manifest.py`, adding a `_PROMOTED` handoff
declaration for the four `review` fields. Without it pyverify reads the
promotion as **drift** against Production's landed pack and fails; with
it, the disagreement is a recorded handoff carrying its own reasoning.

## 3 · The four transitions

| shell | was | now | form verdict and its condition |
| --- | --- | --- | --- |
| `shell_hall_transit` | `pending` | **`pass`** | FORM PASS with three collar `grapple_point` offers and the collar walking loop closed — **both completed**; the mandatory route is still walkable with no package |
| `shell_plenum_helix` | `pending` | **`pass`** | FORM PASS — top-entry / bottom-exit preserved: entry still at y = 68, exit at the floor |
| `shell_yard_gantry` | `pending` | **`pass`** | FORM PASS — ~16 m height preserved: still 84.0 × 16.0 × 52.0 m |
| `shell_span_basin` | `pending` | **`pass`** | FORM PASS — the intentional one-way drop preserved, unchanged |

The audit's single finding — the yard's launch pad at y = 0.5 over a
floor whose top is 0.00 — was corrected at `466fd4e`, before this flip.

## 4 · Generated paths changed

Exactly two:

* `godot/content/registry/authored_art.json`
* `godot/content/SCENE_PLAN.json`

Both regenerated by `tools/export_content_pack.sh`. Nothing hand-edited.

## 5 · Field-by-field delta

The whole generated delta is **eight fields**, and the comparison was run
entry by entry and field by field against `f2b920a`:

```
=== registry field deltas (4) ===
  shell_hall_transit     review                 'pending' -> 'pass'
  shell_plenum_helix     review                 'pending' -> 'pass'
  shell_span_basin       review                 'pending' -> 'pass'
  shell_yard_gantry      review                 'pending' -> 'pass'

=== provenance field deltas (4) ===
  shell_hall_transit     runtime_substitution   'pending' -> 'pass'
  shell_plenum_helix     runtime_substitution   'pending' -> 'pass'
  shell_span_basin       runtime_substitution   'pending' -> 'pass'
  shell_yard_gantry      runtime_substitution   'pending' -> 'pass'

VERDICT: EXACT
```

The check is a gate, not a description: it asserts the delta set equals
those eight pairs, that every transition is `pending → pass` and nothing
else, that the entry set and provenance set are unchanged, that the
registry's top-level fields and `SCENE_PLAN`'s `scenes` list and
`_comment` are untouched, and that no other entry's `review` moved. Any
extra field anywhere fails it.

`runtime_substitution` is the review/provenance presentation field
generated from the same decision — the second of the two places the flip
can reach.

**Unchanged, and proved by that same comparison:** geometry, collision,
scene, traversal, surface, socket, volume, offer, connector, size,
semantic tag, fallback and camera values, on every one of the 21 entries.

## 6 · Asset byte-identity

| | |
| --- | --- |
| source `.glb` under `assets/models/` | **254 files, all byte-identical** (`md5sum -c`) |
| shipped `.glb`, `.tscn`, `.import` under `godot/content/` | **all unchanged** — git reports only the two JSON files |
| `diff_shell_glb.py` vs `f2b920a` | **23 shells, all SAME, 0 malformed** |

A review state is metadata. Nothing was rebuilt into different bytes.

## 7 · Validation

| check | result |
| --- | --- |
| `verify_manifest.py` (Production's `ContentManifest`) | **PASS** — 9 declared handoff changes, no other drift |
| `content_registry.gd` (Production's) | **PASS** — 21 entries, 12 shells now SHIP |
| `verify_collision.gd` | 12 shells, **0 needing attention** |
| Scene / manifest marker parity | **PASS** — 160 markers, 12 scenes, 0 disagreements |
| Flight surfaces, 0.10 m grid | 19 flights, **0 refused** |
| `measure_offers.py` | **PASS** — 24 offers, 0 refused, 0 raised |
| `replay_audited.py` | **PASS** — 12 audited findings all still found |
| `check_art_current.sh` | **PASS** — every generated asset rebuilds byte-identical |

## 8 · Projectiles

`projectile_straight`, `projectile_falling`, `projectile_lobbed` all
remain **`pending`**. That reversal is Production's and flipping it back
is an owner decision, not a maintenance one. Every other content review
state is unchanged; the six fixtures stay `pass`.

## 9 · No content contract changed

Nothing but review metadata moved. The played Zone is untouched, the
digest is unchanged, and **Wave 2 has not started** — the verdict Wave 2
was waiting on has arrived and it is a promotion, not an instruction to
continue. Wave 2 needs its own owner brief.

## 10 · Art promotion commit

**`ab74f5e`** — *All twelve room shells are pass* — pushed to
`claude/archipepsi-art`. (This report names it after the fact; the
number above is the real one.)

---

## What `pass` does not say

These four carry `rail_route`, `launch_source` / `launch_target` and
`grapple_point` offers. Those are **declarations reserved against a
player-facing movement-package consumer that is not implemented.**

> A passing shell can be placed, entered and **walked end to end today**
> with nothing installed — that is the condition the offers were allowed
> to exist under, and it still holds. Nobody can ride its rail.

Two different readinesses. Reading one as the other is exactly the
conflation that put the projectile substitutions where they are: a batch
verdict was read as a shipping decision. The doctrine comment above
`SHELL_REVIEW`, this report, the handoff and the frontier all now say so
in the same words.

## Corrected the same day — both of these are resolved

This section originally listed req 40 and `launch_source.radius` as open.
**Both were already settled**, and so was a third thing this lane had
raised. Corrected here rather than left standing, because a report that
names a resolved question as a blocker is how a blocker gets inherited.

* **Req 40 — RESOLVED.** `ShellValidator` is kind-aware through
  `TraversalLaw`: it no longer applies jump bounds to continuous walks or
  to ramps. Implemented **before** the promotion, so none of these four
  rooms was ever refused by it at the point they passed.
* **`launch_source.radius` — RESOLVED**, settled at Production `833fe80`
  and guarded at `7e13f44`. `launch_source.position` is the exact
  foot-contact launch origin; `radius` **reserves space for the
  constructed pad**; it is **not** a disc of possible ballistic origins.
  So the reading Art measured against is the correct one and all four
  pads are right as authored — the larger moves the disc reading would
  have wanted are not needed, and not doing them on a guess was the right
  call.
* **The plenum collar convex-disc defect — RESOLVED.** Each collar ships
  as twelve convex ring sectors, the holes are physically open, and the
  independent audit verified the decomposition.

## The one milestone that remains, and it is intentional

**The player-facing movement-package consumer**, which is Production's.
Shell promotion does not implement rails, launches or grapples in
ordinary gameplay. Every one of the twelve shells can be placed, entered
and walked end to end today with nothing installed; the `rail_route`,
`launch_source` / `launch_target` and `grapple_point` offers are
reservations against that consumer. That is the state they were authored
under and it is not a defect.

**There is no Art-side blocker.**
