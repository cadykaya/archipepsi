# ART FRONTIER — where the art lane is, right now

**Read this first on every heartbeat.** It is the cheap wake-up state for
the Archipepsi art lane. Everything else in `docs/art/` is reference; this
file is the only one that says what to *do next*.

---

## THE GATE — read before doing anything

> **Style Lock Batch 001 is NOT approved. Mass asset production is blocked.**

Nobody but the owner can move an entry in `ART_REVIEW.md` from PENDING to
PASS. Until that happens the heartbeat may only do objective work.

---

## Status

- Branch: `claude/archipepsi-art`, based on `claude/archipepsi-build-inzshp`.
- Phase: **A–D in progress** (packet, inventory, toolchain, Batch 001).
- Owner review: **not yet requested.**

## In flight this session

Toolchain and Batch 001 are being built. See the commit log.

## Next

Finish Batch 001 and the packet documents, then stop and request review.
